# ============================================================
# OPTIONAL / NOT-YET-INSTALLABLE DETECTORS
# ============================================================
# These packages are NOT installable from public PyPI in their pinned forms,
# so they are kept out of requirements.txt to keep the base install green.
#
#   - sensitive-word-filter-cn: no PyPI release exists
#   - profanite 0.1.10: only 0.1.9 exists
#   - badwords-py 2.3.1: no installable artifact for this platform (2.1.0)
#   - safetext 0.3.3: requires lingua-language-detector==2.1.1 (does not exist)
#   - profanity-filter2 1.4.3: requires ordered-set-stubs (does not exist)
#   - datasketch 2.0.0: installable, but MinHash semantic matching needs a
#     pre-built toxic-signature database; not wired into the pipeline
#
# The MultiLanguageDetector guard-wires safetext, sensitive-word-filter-cn,
# and profanity-filter2 (import-guarded adapters). If a private index or
# mirror hosts them, install with:
#
#   pip install -r requirements-extra.txt
# ============================================================
sensitive-word-filter-cn==0.1.6
profanite==0.1.10
badwords-py==2.3.1
safetext==0.3.3
profanity-filter2==1.4.3
datasketch==2.0.0

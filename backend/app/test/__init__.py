"""Interactive test workbench package.

Exposes the moderation pipeline internals through the ``/test`` endpoints so
the developer console can inspect traces, run load tests, and tune runtime
settings without touching production logic.
"""

# Root environment file.
#
# The moderation service reads its configuration from the backend working
# directory. The complete, authoritative list of environment variables lives in:
#
#   backend/.env.example
#
# Copy that file to backend/.env and adjust the values for your deployment.

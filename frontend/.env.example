# Base URL of the moderation service backend.
# Leave empty to use the same origin: in production the FastAPI service
# serves the built frontend and the API on one port; in dev the Vite proxy
# forwards API calls to the backend.
VITE_API_BASE_URL=
